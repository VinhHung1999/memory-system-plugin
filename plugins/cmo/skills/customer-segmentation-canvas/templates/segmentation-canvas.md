# Segmentation Canvas — ${COMPANY} (${STAGE})

## 1. Market Description
${MARKET}

## 2. Segmentation Level
Recommended level: ${LEVEL} (mass / segment / niche / segment-of-one)
Rationale: ${RATIONALE}

## 3. Criteria Type
Primary: ${PRIMARY_CRITERIA} (geo/demo/psycho/behavioral)
Secondary overlay: ${SECONDARY_CRITERIA}
Why this combination: ${REASONING}

## 4. Candidate Segments

### Segment A: ${NAME}
- Who: ${WHO}
- Key need (JTBD): ${JTBD}
- Channel hypothesis: ${CHANNEL}
- Sizing: ${SIZE} companies x ${ARPC} ARPC = ${TAM} addressable
- 5-filter MAPDA check: M ${M_A} / A ${A_A} / P ${P_A} / D ${D_A} / A ${ACT_A}

### Segment B: ${NAME}
- Who: ${WHO}
- Key need (JTBD): ${JTBD}
- Channel hypothesis: ${CHANNEL}
- Sizing: ${SIZE} companies x ${ARPC} ARPC = ${TAM} addressable
- 5-filter MAPDA check: M ${M_B} / A ${A_B} / P ${P_B} / D ${D_B} / A ${ACT_B}

### Segment C: ${NAME}
- Who: ${WHO}
- Key need (JTBD): ${JTBD}
- Channel hypothesis: ${CHANNEL}
- Sizing: ${SIZE} companies x ${ARPC} ARPC = ${TAM} addressable
- 5-filter MAPDA check: M ${M_C} / A ${A_C} / P ${P_C} / D ${D_C} / A ${ACT_C}

## 5. Strategy Recommendation
Kotler strategy: ${STRATEGY}
Reasoning: ${STRATEGY_RATIONALE}

## 6. Validation Plan
- Customer interviews per segment: ${INTERVIEWS} (minimum 5-7)
- Quantitative validation: ${QUANT_PLAN}
- Decision date: ${DECISION_DATE}

## 7. Anti-Segments (we explicitly are NOT for)
${ANTI_SEGMENTS}
