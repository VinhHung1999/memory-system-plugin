# Marketing Performance Scorecard — ${QUARTER}

**Top-line:** ${TRAFFIC_LIGHT} ${ONE_LINE_VERDICT}

---

## Top 3 metrics that matter

1. **${TOP_METRIC_1}**: ${VALUE_1} (target ${TARGET_1}, variance ${VARIANCE_1})
2. **${TOP_METRIC_2}**: ${VALUE_2} (target ${TARGET_2}, variance ${VARIANCE_2})
3. **${TOP_METRIC_3}**: ${VALUE_3} (target ${TARGET_3}, variance ${VARIANCE_3})

---

## GREEN zone (monitor)

| Metric | Current | Target | Variance | Note |
|---|---|---|---|---|
| ${METRIC} | ${CURRENT} | ${TARGET} | ${VARIANCE} | ${NOTE} |

---

## YELLOW zone (diagnose + prep action)

| Metric | Current | Target | Variance | Hypothesized cause | Action prep |
|---|---|---|---|---|---|
| ${METRIC} | ${CURRENT} | ${TARGET} | ${VARIANCE} | ${HYPOTHESIS} | ${ACTION_PREP} |

---

## RED zone (intervene now)

| Metric | Current | Target | Variance | Root cause | Corrective action | Owner | Due |
|---|---|---|---|---|---|---|---|
| ${METRIC} | ${CURRENT} | ${TARGET} | ${VARIANCE} | ${ROOT_CAUSE} | ${ACTION} | ${OWNER} | ${DATE} |

---

## KPI hygiene flags

- ${ORPHAN_METRIC_LIST}
- ${STALE_METRIC_LIST}
- ${VANITY_METRIC_LIST}

---

## Asks for board

1. ${ASK_1}
2. ${ASK_2}

---

## Appendix — Activity-vs-impact pairing

If activity metrics still appear on the operator dashboard, list their impact twin here so the board sees the connection (NOT in the main scorecard above).

| Activity metric | Business-impact twin | Status |
|---|---|---|
| ${ACTIVITY} | ${IMPACT_TWIN} | ${ON_TRACK_OR_NOT} |
